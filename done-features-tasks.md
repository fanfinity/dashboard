# Done / features / tasks log

Reverse-chronological, one line per entry: `- YYYY-MM-DD: <concise description>`. Append
whenever a feature or task is added, finished, or stopped — see CLAUDE.md's "Done-features log"
section for the full convention. Cleared weekly after being shared to Slack.

- 2026-09-06: Balanced the Dashboard diagram card's vertical breathing room. The hub's halo is absolutely positioned and contributes no height, so on a short diagram the row ended flush with the circle and the glow spent the card's whole bottom padding — about 5px to the border against 20px above the headings. The hub cell now reserves the halo's own 16px, so top and bottom measure 21px and 22px.
- 2026-09-06: Fixed the Dashboard diagram's circle hanging out of its card, and traced it to a cascade collision that was also breaking the login screen. The circle had been centred with a measured `translateY`, which aligns it and changes no layout box, so nothing grew to contain it; the hub and both card stacks now share a real second grid row and are all `self-center` in it, so their midpoints are equal by construction (verified at four shapes: one-each, empty, bare, and three-sources-one-destination — all three midpoints identical in every one). Getting there needed `display: contents` on the two `<section>`s, which turned out to be a dead class: Quasar ships the HTML5 normalize line `article, aside, … section { display: block }` UNLAYERED, so any layered display utility on those eleven tags loses. Logged as collision #10, and grepping for the pattern found two more — `max-lg:hidden` on `LoginPage`'s dark pitch panel and on the design-system rail, neither of which had ever hidden. The login one was shipped: 46% of every window under 1024px went to a panel meant to be gone there. All three carry the important suffix now. `pnpm smoke:dist` 52/52.
- 2026-09-06: Fixed the Dashboard topology's alignment, and found a dead class doing half of it. The hub and its placeholder rails were positioned at the centre of the whole diagram box — column headings included — while the cards they were meant to join sat lower and at two different heights, so three things floated at three unrelated levels. The rails are measured wires now (the empty placeholder registers an anchor like any node), and the hub's vertical centre is the midpoint of the two card stacks, with its radius read off the rendered circle rather than hardcoded. Separately: `max-@min-[52rem]:` emits no CSS at all — Tailwind generates no `@container not (width>=52rem)` block for it — so ten utilities across `FlowTopology`, `FlowChain` and `FlowWire` had never applied, which is why a stacked chain's connector stayed horizontal and the topology drew its wires diagonally across the stacked columns. All three are on `@max-[52rem]:` now, the form the rest of the repo uses. `pnpm smoke:dist` 52/52.
- 2026-09-06: Rebuilt what the Dashboard shows an account with no sources, and improved the flow diagram everywhere it is drawn. New `WorkspaceReadyBand` (numbered, one sentence, `Connect a source`) above the picture; the header stands its own primary action down while a band is up, so the empty sources column can carry the `+ Add source` the old "no button in either empty column" rule forbade; the bare `EmptyState` for a genuinely empty workspace is gone in favour of the topology itself; the fourth stat card reads `Source setup / Not started` rather than a delivery percentage over no attempts; and the amber "1 thing needs your attention" for the auto-provisioned warehouse having no pipe is suppressed before the first source, since the only action was the one the band already names. Diagram: bigger hub carrying the wordmark with a two-layer halo, a dashed rail between the columns when there is no pipe to draw, `ClickHouse` read off the record as the destination's own line, and a `Ready` chip instead of a grey `Idle` on a warehouse with nothing yet to receive. Plus a `Hello <name> 👋` greeting through a new `#eyebrow` slot on `PageHeader`. `pnpm smoke:dist` 52/52.
- 2026-09-06: Fixed the Dashboard header's two actions drawing as two different controls, in the kit rather than on the page. Two causes, both measured on `/design-system`: Quasar's unlayered `button { font: inherit }` (the shorthand, so size, line height AND weight) killed every type utility on any `SfereButton` that renders as a `<button>` while leaving the `router-link` ones alone — so `Refresh` was 400 weight where `Connect a source` was 600 — and a bordered variant stood 2px taller than a borderless one on a padding-sized pill. Type utilities now carry the important suffix, every variant reserves a border (`border-transparent` where it should not show), and the sizes pin `SfereIconButton`'s own 36/40/44 ramp, so the kit's Density claim that an input, a select and a medium button share one 40px row height is finally true (`md` had been 43px). `TabNav`'s tabs had the same dead `font-medium` and now render at 500; logged as cascade collision #9.
- 2026-09-06: Removed the Dashboard's three-step setup tracker entirely — `SetupProgressPanel.vue` is deleted, and with it the page's `firstRun` branch, so there is no `<h1>` swap to "Let's get your activity data flowing", no suppressed subtitle and no gated header actions. The topology one card lower already answers "how far am I?" from the reader's own records, and `Connect a source` is in the header on every state. `SetupReminderStrip` on Sources, Destinations and Pipes is the only setup tracker left; its `See full setup progress` link came off with the panel it pointed at.
- 2026-09-06: Closed the one sign-in gap the tenant-per-account flip did open on our side: `/v1/me` now builds its memberships from the token's claims, so a login whose `account_id`/`role` claim never got stamped answers `200` with an empty array instead of a `403` — which the dashboard read as success and turned into an empty, working-looking shell. An empty memberships array now counts as "no account" on both the sign-in and the sign-up path, so the reader is told what happened instead of being bounced back to `/login` in silence. Also pointed `.env`'s `VITE_API_BASE` at `api-staging.sfere.io` rather than the retiring `fanfinity.io` host.
- 2026-09-06: Fixed rotating a browser write key not changing the setup snippet — `Source.write_key` is written once at create and no backend route moves it, so minting a new key changed nothing on the Setup instructions tab and revoking the old one left the page handing out a key ingest rejects. A key minted in this session now wins everywhere a snippet is drawn (one `snippetWriteKey()` reader shared by the install guide, the Web SDK panel and the create flow), held in memory only because the value is shown once; and the record's key is checked against the live key list before it is printed, so a key revoked in an earlier session gets a warning and a `your-write-key` placeholder instead of a dead key in working-looking code. The reveal dialog also now shows the `keyId:secret` pair rather than the bare secret the endpoint returns, which is the form Jitsu actually accepts.
- 2026-09-06: Verified the write-key rotation fix against real staging through the UI — minting a browser key on a live source rewrites both the Web SDK panel's snippet and the Setup instructions guide to the new `keyId:secret` pair, the "showing the key you just created" banner appears, and revoking that key puts both back on the source record's key. The keys minted while checking were revoked afterwards, so the account is as it was found.
- 2026-09-06: Sign-in was broken by the backend's Firebase tenant-per-account flip, not by anything here — it was a greenfield reset with no backfill, so every pre-flip account including `SMOKE_EMAIL` answered `401` and had to be registered again. The login wire contract never moved, so nothing needed rewiring; the account has been recreated and sign-in is verified end to end (token, `/v1/me`, shell render, reload, cold-loaded deep link, and a signed-out deep link bouncing to `/login` with its redirect). A 5xx from registration now shows one written sentence instead of the 400-character Google metadata-service traceback the endpoint returns.
- 2026-09-06: Stopped a source that already has events from being asked to prove it — the Web SDK setup panel and the shared install guide now ask the backend on arrival instead of waiting for a click, so a live source shows "Receiving events" (or hides its go-live card entirely) rather than sitting on "Waiting for first event" / "2 steps to go live". The panel's "Check for events" button is no longer disabled until you press **Copy snippet**, which forced anyone whose tag was already installed to copy a snippet they did not need in order to unlock the check; and both steps were per-browser `localStorage` flags, so the same live source read as unfinished on a second machine. One `checkSourceEvents()` helper now owns the question for all three callers (panel, guide, first-run verify beat), including the rule that a `400` means "no event log yet", not a failure.
- 2026-09-06: Gave Destinations the same prototype rows as Sources — the All/Enabled/Paused filter tabs, the search box and the sortable table are gone, replaced by one bordered card per destination with the prototype's five cells: name and type, how many pipes deliver here, who provisioned it, a status chip, and the kebab. The pipe count is the real `pipe_count` from the dashboard aggregate rather than the fixture field the old column was dropped for, and the two screens now share one reader (`useFlowActivity.js`) so they cannot disagree about it. The third cell says "Sfere managed" rather than the prototype's "Included", which is a billing claim nothing measures, and it shares its derivation with the detail screen's Provisioning card. A destination turns amber only when it is enabled, has a pipe pointing at it, and the window counted zero deliveries — a null delivery count means the analytics store did not answer and never colours a row.
- 2026-09-06: Rebuilt the Sources list against the prototype — four tabs (Event streams, Connectors, Zid stores, Salla stores) and the All/Enabled/Paused pill row are gone, and the table became one bordered card per source carrying the prototype's five cells: name and type, pipe count with where it delivers, activity, a status chip, and the kebab. The three cells the `Source` record cannot fill are read from one dashboard-aggregate call (`useSourceActivity.js`) rather than invented, so a source the read missed prints `Not known` instead of a confident `0`, and a source that is switched on but counted zero events is the one that turns amber. The prototype's "8 sec ago / Last activity" is the one thing not ported: no endpoint carries a per-source last-event timestamp, so that cell says the events measured in the last 60 minutes instead. The connector catalog is a screen again at `/connectors` (a sub-screen of Sources, so no new sidebar row), because the tab holding it went with the rest.
- 2026-09-06: Cleaned up the Dashboard header and retired the finished setup tracker — the purpose line no longer ends `· updated 17:10` (682px against PageHeader's 672px measure, which orphaned the time onto a second line), the clock is a muted span beside Refresh, and the two header actions lost their `size="sm"` so they match the 40px `#actions` row every other screen draws. The setup panel now hides itself once the chain is complete OR the arrival was finished, instead of sitting at 3/3 behind a per-browser `Hide` link above a topology saying the same thing; the onboarding half is what covers an iOS or HTTP-API source, which is 1 of 3 by design.
- 2026-09-06: Fixed the dashboard staying on the first-run '0 / 3 done' setup screen after onboarding finished — Home now refetches when the arrival closes, and a parked arrival keeps its resume band even though it created a source.
- 2026-09-06: Fixed the staging smoke test that 401'd after the backend flipped to one Identity Platform tenant per account (greenfield reset left no accounts, so `SMOKE_EMAIL` no longer resolved). `smoke.mjs` now self-provisions via `ensureSmokeAccount()` — probes `POST /v1/auth/token` and registers ONCE only on a 401 (register mints a fresh tenant per call, so it must never run blindly), then drives the existing UI login; `deploy-staging.yml` passes `VITE_API_BASE` to the smoke step so the script has the backend base.
- 2026-09-06: Rebuilt the sign-up onboarding as the prototype's full flow — seven beats on one full-page surface (welcome, category, platform, store authorization, install, confirm, provisioning, ready) instead of three beats and a hand-off to `/sources/new`: the arrival now creates the source itself with a derived name, shows the real write key through the same `SourceInstallGuide`, runs the real `listSourceEvents` check, and reports what `POST /sources/provisioned` actually built, naming the real ClickHouse destination and pipe. What the prototype fakes is not faked: "Send a test event" only appears for store sources (as the real `POST /sources/{id}/test`), a source type the backend provisions nothing for gets an honest "no destination yet" branch instead of a ticked warehouse, and one completed event check unlocks the rest of the flow whether or not anything has arrived, since a snippet that is not deployed yet is the normal case. The onboarding record is v5, persisting the current beat and the created source id so a mid-flow reload resumes instead of creating a second source.
- 2026-09-06: Took the Warehouse group off the sidebar — the warehouse is a destination now (the backend provisions a ClickHouse destination per source, and its tables and SQL console are tabs on the destination detail screen), so a second rail row pointing at `/dwh-connections` said it lived somewhere else; the `warehouse` feature key stays active, so those three routes still render their real pages rather than `ComingSoonPanel`.
- 2026-09-06: Fixed the first-run arrival going blank the moment somebody pressed `Start connecting my data →`: each beat's SFC opens its template with a comment, so in dev its root compiles to a fragment rather than an element, and unmounting a fragment root skips the leave transition — under `<transition mode="out-in">` that meant `afterLeave` never fired, `isLeaving` stayed true and the surface rendered its empty placeholder and nothing else. Each branch is now a plain `<div>` the overlay itself owns, so no beat component's root shape can break the swap; the same `out-in` mode also made the beat's focus hand-off a silent no-op, so focus now moves on the transition's own `@enter` rather than after two `nextTick`s.
- 2026-09-06: Added `Restart onboarding` to Settings → General, so somebody who has already answered the arrival can replay the three-beat welcome — it re-arms the record (clearing the recorded category and platform), confirms first because the surface it opens is full-page and persistent, and navigates to Home, which also meant fixing `MainLayout`'s session-only `arrivalFinished` flag that made the same restart work or silently do nothing depending on whether the reader had reached Settings by a cold load or a click through from Home.
- 2026-09-06: Fixed the Live Events filter row, whose two date fields overlapped the controls beside them — a native `datetime-local` has a ~222px min-content width at Inter 14px and a grid item cannot be squeezed under its own min-content, so six equal `1fr` tracks under ~1700px of container pushed the pickers on top of their neighbours instead of shrinking them. The two dates now hold pinned 16rem tracks while Source, Status and Search share the rest, with `min-w-0` on every cell; measured overlap-free from 520px to 1920px, and all six fit on one line from 72rem of container up.
- 2026-09-06: Fixed a blank onboarding screen: the platform beat was the overlay's `v-else`, so any step that was not welcome or category landed on it, and its fallback for a category with no platform group was an empty object — which rendered as a blank full-screen surface with only the wordmark and Skip on it, and no Back, since the footer is inside the same panel. It is now mounted only when it has a group to show and falls back to the category question, with a second guard in the component itself.
- 2026-09-06: Made the first-run arrival sign-up-only — it was keyed on an absent `localStorage` record, so it was a property of the browser rather than the account and covered the dashboard for any existing user signing in on a new machine, in a private window, or after clearing storage; `LoginPage`'s sign-up branch is now the only thing that arms it. Also put the skip control on the welcome beat, which had exactly one control and it pointed forwards, so a `persistent` full-page surface had no way out. Verified against staging with two freshly registered accounts: both hand-off paths, pause and resume, and a sign-in and wiped-storage sign-in that stay clean.
- 2026-09-06: Rebuilt first-run onboarding as the prototype's full-page light flow — three beats (what Sfere does → where your customer activity happens → which platform) on one `maximized` surface instead of an 860px dark card, with the welcome's touchpoints → Sfere → activity diagram, and a `?template=` hand-off so picking Zid or iOS lands on `/sources/new` with the platform already selected. Skipping is now a pause rather than a dismissal: it remembers the beat, and the Dashboard carries a resume band that reopens the flow there. The prototype's install/verify/provision/ready steps were deliberately not rebuilt — they already exist on `/sources/new` against real endpoints, where the prototype's were two `setTimeout`s.
- 2026-09-06: Removed the duplicate `Connect a source` button from the Dashboard topology's empty sources column — the topology only renders in the branch where `PageHeader` already carries that exact action, so the two showed together; every add affordance in this app is a header action, and the rule is now written down beside the diagram.
- 2026-09-05: Rebuilt Live Events on the kit (it was the last screen with a raw `<h1>`, toolbar, table and drawer), rebuilt the Functions screens around one shared test workbench with real result/logs/duration from `POST …/functions/{id}/test`, gave Destinations the included-warehouse hero plus a five-tab detail screen, and gave Pipes an intro band, summary cards and a Visual/List toggle over `PipelineDiagram`. Along the way: the Destinations Pipes and "Delivered (1h)" columns and the fixture-only "Upgrade available" claim are gone, there is deliberately no Edit pipe screen (`PipelineUpdate` accepts `is_enabled` alone), and the delete copy on both screens stopped promising a trash that `DELETE` does not provide.
- 2026-09-05: Ported the updated prototype into the app: first sign-in is now a two-beat dark overlay (what Sfere does → where your customer activity happens) that hands off to `/sources/new?intent=…`, replacing the persona question — `personas.js`, `navOrder.js`'s ordering, the role picker and the per-role sidebar/dashboard orderings are all deleted, so the rail is the authored order for everybody; the Dashboard became a source → Sfere → destination topology plus four measured counts, on a new shared `src/components/flow/**` family (one diagram implementation for Home, Pipes, pipe detail and route previews) rather than a fifth hand-rolled SVG; and list screens gained a dismissible `IntroBand`. Every unbacked number the prototype printed (per-pipe last activity and success rate, per-source last activity) was omitted rather than rendered as a confident `0`.
- 2026-09-03: Fixed the red staging deploy that #98 introduced: `/profiles/:id` was the one detail screen reading a single record over the network, so on staging (which has no profiles) its fixture `smokeParams` id was an honest 404 that Chromium logged and `smoke.mjs` failed on — attributed to whichever route happened to be current, which is why the failing route moved between runs. The screen is now `smokeMockOnly` and the walk skips it in real mode and says so, rather than adding a 404 to `IGNORED_CONSOLE`.
- 2026-09-03: Added a guided walkthrough that actually points at things — a reusable `SpotlightTour` primitive dims the window, rings the control to use next and floats a callout beside it, driven by the page that owns the state (`useGuidedTour().show()` plus a `data-tour` anchor, steps as data in `src/config/tours.js`); it is armed by the arrival card's CTA and walks pick-an-intent → create → check-for-events on `/sources/new`, ending on the first real event beside the confetti.
- 2026-09-03: Turned the role question into a two-beat arrival that hands every role to their first source — answering now swaps the same dark card for the three setup steps and one button onto the first undone one (per-role copy, marks only once the reads answer), and added a reusable `SfereConfetti` primitive plus `useConfetti().fire()`, mounted once in MainLayout and fired when the backend actually provisions a pipeline and when the first real events arrive.
- 2026-09-03: Dropped the Pipes column from the Sources list — `pipeCount` is a `sources.json` invention the backend's `Source` does not carry, so the cell was blank on every row on a real account under a header promising a count. The same column on Destinations and the `Pipes` stat on both detail screens still read the same field and were left alone.
- 2026-09-03: Redesigned the two first-run surfaces — the role question overlay now sits on the brand's dark ink canvas (dot grid, purple bloom, on-dark cards, the wordmark and a display-scale heading), the one deliberate exception to the light house dialog string; and the Dashboard's setup tracker became a source → destination → pipe diagram of three connected nodes with locked downstream steps, which now also replaces the plain zero-state card so one surface answers "how far am I?" at 0, 1, 2 and 3.
- 2026-09-03: Landed the UX pass across the list screens and the source create flow — row actions collapsed into one kebab menu (a new `RowActionsMenu` primitive), the ten per-module trash screens became one `/trash` with Sources/Destinations/Pipes tabs and a bottom-menu row, `/sources/new` now keeps a draft of steps 1–2 through a reload and greys out Shopify and Stripe as coming soon, the Dashboard's zero state says one thing and offers one button, the back link is bordered at rest, the dead "Upgrade available" badge and filter tab are gone from Sources, and every list page caps its content at 1400px.
- 2026-09-02: Made the S3 destination Region a dropdown of Jitsu's exact `region` enum (was a free-text box, so any value Jitsu doesn't recognize failed its zod validation and the destination was silently not created in Jitsu); kept admin-dashboard's registry in sync.
- 2026-09-02: Trimmed the sidebar to rows you can actually use and folded three surfaces into Settings: switched-off modules are now hidden rather than rendered as inert `Soon` rows (the route gate is unchanged, so `pnpm smoke:dist` still walks all 58), Secrets and Authorizations became tabs on `/settings` instead of permanent bottom-menu rows, the role picker moved into the General tab, and the six workspace-only tabs are offered only in Demo data mode where they have something to show.
- 2026-09-02: Fixed dialog corner radius app-wide: Quasar's unlayered `.q-dialog__inner > div { border-radius: 4px }` beat every layered `rounded-*`, so all seven Sfere-era dialogs declared a 16px radius and rendered at 4px — squarer than the cards inside them. Added the important suffix (`rounded-sfere-xl!`) to all seven and documented it as cascade collision #8; the persona overlay also picked up the house dialog surface string, replacing an improvised `rounded-xl`/`shadow-lg`.
- 2026-09-02: Reworked the first-run role question overlay — demoted Skip from a button to a quiet control so the three answers carry the weight, dropped the repeated per-card "Start", merged the two reassurance sentences into one accurate subtitle, and acknowledged skipping with a toast.
- 2026-09-02: Made the persona answer actually change the app — the sidebar's order and the Dashboard's block order are now data on each persona (`nav`/`home` in `src/config/personas.js`), applied by `src/lib/navOrder.js` and `DashboardHomePage.vue`, front-loading so a bad key can misplace a row but never drop one; marketer leads with Profiles and a collapsed issue count, analyst leads with events and gets Warehouse at the top of COLLECT, engineer is unchanged. Also cleaned up the persona question card: dropped the number chips, the description line, the fictional time estimate and the footer divider, and centred what is left.
- 2026-09-02: Fixed a real-mode smoke failure that surfaced as a 404 on `/pipes/pipe_web_to_snowflake`. The destination detail page eagerly fired the per-id table read (`GET …/destinations/{id}/tables`) from an `immediate` watch keyed on the raw route id, so on the smoke walk it fetched the mock fixture id `dst_snowflake` — which 404s on the real account — and, because that read awaits `waitForAccount()`, the 404 resolved late enough to land in the _next_ route's console bucket. Keyed the watch on the resolved `destination` record instead, so a nonexistent id is treated as "empty" and never triggers a request, matching every other detail page's collection-lookup pattern.
- 2026-09-02: Adopted backend PR #16's OpenAPI as the agreed contract, regenerated the API client, and verified the 45edb4f wiring against it field by field (see docs/contract-check-pr16.md); fixed the one break it found by pointing source creation at `POST /sources/provisioned`, since the plain `POST /sources` no longer builds the destination and pipeline that `/sources/new` step 3 reveals.
- 2026-09-02: Zid sources now start with the authorization, and cannot be created without one. Picking the Zid template on `/sources/new` puts a "Zid setup" panel at the top of the form — the same numbered-step look as the existing ZidSetupWizard — with **Authorize with Zid** / **I've authorized**, and Create source stays disabled until the store has granted access. The store id is no longer typed: it is read off the grant (`…/zid-connections`), which is also what makes an empty id mean "not authorized yet". This replaced a free-text store-ID box that asked for an id nothing checked — `connect-zid` 502s without OAuth tokens, so an unauthorized store gave a source that could read nothing and only said so three screens later. The authorization URL is now fetched from the backend (`…/zid-authorize`, and `zid-status`'s `authorize_url`) rather than hand-built: it is a single-use start hop that sets the signed cookie linking the handshake to this account, which a browser-built URL cannot do, with the old `/redirect-url` entry kept as a fallback since staging serves that today and 404s all three PR #16 routes. ZidSetupWizard also moved onto step 3 above the provisioned-pipe panel, and the celebratory overlay is suppressed for Zid, because that chain is dry until webhooks and the first sync have run.
- 2026-09-01: Fixed the New notification channel dialog rendering as two clipped columns instead of one scrolling card: Quasar's unlayered `.flex` sets `flex-wrap: wrap`, so a height-capped `flex-col` wraps into a second column rather than scrolling, and `flex-nowrap!` (important suffix) is what beats it. Applied to all four capped-height cards, since the other three were the same latent bug.
- 2026-09-01: Wired the dashboard to backend PR #16 (`feat: jitsu proxy`) — 62 endpoints across 19 screens that gained live data, four new screens (Functions list/create/detail, Profile builders), six new tabs and panels (source write keys, ingest/strict mode, sync setup and run logs, destination tables and query, Zid connections, ingest domains, notification channels, connector images, API tokens), eleven new composables and six mock fixtures so Demo mode still shows the shape. Three backend placeholders are reported as unmeasured rather than as a confident `0` (health queues, per-window diagram counters, identifier-type coverage), and `scripts/smoke.mjs` now settles the network twice before asserting because a single `networkidle` was resolving before the account-scoped fetches were even issued, which made the real-mode run fail a random subset. Merge the backend PR first: ten of these endpoints do not exist on staging yet, so real-mode smoke is 44/60 until it lands (mock mode is 60/60).
- 2026-08-31: Checked backend PR #16 against the four drafted backend asks, against the router source at the PR head rather than the spec's status labels: it closes three of the five source-Settings items (write-key rotation, server-to-server keys, strict mode) and leaves the source trash, account ClickHouse and auth/onboarding asks untouched. Found that `template_id` is already stored and mapped but silently dropped because the spec's `Source` schema lacks the property, so one backend line closes a QA finding with no frontend change; that plus two smaller findings are now Problems 4–6 in the review doc. Nothing is wired yet — the PR has not merged.
- 2026-08-31: Worked the signup/sign-in QA report — both forms now say why they refused instead of leaving a dead button, sign-up enforces a real password rule with a strength meter and a show/hide toggle, a personal email gets a non-blocking warning, /signup is its own route, the persona modal fades out, and the "Setting up your account" animation no longer greets returning users. Password strength, email verification, domain-matched workspaces and password reset all need backend endpoints that do not exist; the ask is in todos/backend-ask-auth-onboarding.md.
- 2026-08-31: Fixed the four general QA notes — the nav toggle under 1024px was white-on-white and is now a visible SfereIconButton, paragraph rows no longer sit 8px above the control beside them, icon-button tooltips stop being clipped at the right edge, and em dashes are gone from UI copy in favour of sentences plus a four-word empty-value vocabulary.
- 2026-08-31: Worked the Sources QA notes — install tabs now narrow to the methods that fit the source, snippets point at console.sfere.io, the off toggle no longer reads as disabled, Delete says what it does instead of promising a trash, and every Settings action with no endpoint behind it is disabled rather than toasting.
- 2026-08-29: Row-level Pause/Enable on the twelve list screens that have one now opens the same confirmation dialog Delete already did, each with copy naming what stops and where, so no state change on a table row fires straight off the click any more.
- 2026-08-29: Fixed the destination detail page throwing at render on a stat card that counted a `warehouses` list the managed-warehouse rework had deleted, and rebuilt that row the way the source page's was — Status, Inbound pipes and Created, dropping three fixture-only cards that reported a measured 0 and a literal `vundefined` on every real destination; the inbound-pipes table now reads the account's real pipelines instead of an unwired composable that always answered empty.
- 2026-08-29: Every detail, create and trash screen now carries a back link to the list it came from, rendered by PageHeader off a new `parent` field in the screen manifest — replacing the twenty-odd hand-rolled "All sources" / "Cancel" header buttons that pointed at the same list under four different names, and that only some screens had at all; top-level screens deliberately get none, since the sidebar is their nav.
- 2026-08-29: Source, Destination and Pipe detail headers now use icon-only Pause/Enable and Delete buttons, and both ask for confirmation in a dialog that spells out what the action does.
- 2026-08-29: Creating a web or Zid source now plays a short full-screen overlay animating the source → pipe → destination chain the backend provisioned, with the inline panel kept as the durable record.
- 2026-08-29: Removed the Template tab from the source detail page, along with the "Upgrade available" badge, the "Upgrade template" button and the two template rows in the Overview facts. The backend takes a `template_id` when a source is created and never returns one on the read, so all of it was permanently empty on a real source and only ever rendered off the wider mock fixture.
- 2026-08-29: Fixed the source detail page reporting "CONFIG VERSION vundefined" and three dashes — all four stat cards read fixture-only fields the backend's `Source` never returns, so the row is now Status, Pipes (the same joined list the "Destinations & pipes" tab counts, dashed while that read is in flight or failed) and Created; the Overview facts stopped labelling every real source "Hand-configured" and the Template tab stopped claiming it "was configured by hand" when the backend simply records no template id.
- 2026-08-27: Creating a source now shows what the backend already built for you — step 3 of /sources/new reveals the auto-provisioned ClickHouse destination and the pipe joining it in an animated source → pipe → destination diagram, and the primary action stops saying "Add a destination" for a source that already has one. Measured the behaviour against staging first: only `web` and `zid` sources get a destination and pipe; `event_stream` (iOS/Android/HTTP API) and `cloud_app` (Shopify/Stripe) get a write key and nothing else, so the panel is driven by what it finds rather than by the source type.
- 2026-08-27: Fixed the destination detail page telling every ClickHouse destination it "delivers over the network rather than into a warehouse" — it read `warehouseConnections`, a mock-fixture field with no counterpart in the real schema, so it now reads the real `clickhouse_database` and `config.hosts`/`protocol` and distinguishes provisioned from still-provisioning. `/dwh-connections` now reports "No API yet" instead of claiming there are no connections; the account-level ClickHouse has no API representation at all yet.
- 2026-08-27: Trash and New are now icon buttons in every list screen's toolbar, drawn by two new kit components — `SfereIconButton` (label required, feeding both the aria-label and the tooltip) and `SfereIcon` over a shared glyph registry.
- 2026-08-27: Removed the unlabelled template-version date from every card in the destination catalog — it is only meaningful next to an existing destination's version, which is where the upgrade badge already shows it.
- 2026-08-27: Dropped ClickHouse from the destination create catalog — we provision it with every plan, so it is now a highlighted notice above the grid instead of a card offering to create one.
- 2026-08-27: Fixed the write key never appearing after creating a source — `useSourcesAPI().create()` returned the backend's raw snake_case payload, so the install guide read `writeKey` off a record that only had `write_key` and showed "provisioning…" plus a `your-write-key` placeholder for a key that had been issued.
- 2026-08-27: Fixed the red CI on the ui-overhaul PR — a Markdown hard wrap had landed inside an inline code span in `docs/ui-conventions.md`, which ends the list item and makes oxfmt non-idempotent, so `oxfmt --check` failed even straight after running the formatter.
- 2026-08-27: Fixed ~1850px of dead space under step 1 of /sources/new — a `@container` inside a Quasar wrapping column flex was measured at min-content height.
- 2026-08-27: Submit rows across the create forms are now sticky to the bottom of the viewport via a new `StickyActionBar` primitive.
- 2026-08-27: replaced the header account chip's bundled photo — which showed the same stranger's face to every signed-in user — with a stroke-drawn person mark on the brand tint.
- 2026-08-27: fixed the spacing, alignment and icons on the source intent picker — redrew the six marks as real multi-element SVGs (the server rack read as a hamburger menu, the plug as a light bulb), and found two more Quasar/Tailwind cascade collisions doing it: `mt-*` on a `<p>` computes to zero so the cards' whole spacing rhythm had never rendered, and an `auto-fit` grid inflated 219px cards to 637px.
- 2026-08-27: made the post-auth "setting up your account" transition a fixed 2.5s instead of a random 1.1–2s, so it is long enough to read rather than flashing past.
- 2026-08-27: fixed the Pipes screens rendering fixture-only fields against the real pipeline API — the backend's Pipeline record has no `version`, no end names and no delivery counter, so `usePipes` now joins each pipe's source and destination in from the live Sources/Destinations collections, the "vundefined" Version card is gone, and the fabricated "0 deliveries" and "Pass-through" now read as unknown, and the Sources list's events/hour column no longer reports a measured 0 for a counter the backend never sends.
- 2026-08-27: built the screens from the onboarding proposal — Team & roles (with the domain-match approval queue) and Billing as a new ACCOUNT sidebar section, a guided three-step source flow (intent picker → details → multi-platform install & confirm), a derived setup tracker on the Dashboard with one-line reminders on Sources/Destinations/Pipes, source-detail Settings and Destinations & pipes tabs, a 29-template destinations catalog grouped by category with Included/Add-on marks, a redesigned login and an animated post-auth "setting up your account" transition.
- 2026-08-27: made the connector catalog connectable — picking a card now opens a real credentials form (named fields for Firebase, MongoDB, Shopify, Stripe and GA4, generic JSON otherwise) with a sync schedule and working validation, instead of a "coming soon" toast.
- 2026-08-27: fixed PageHeader and ToolbarSearch so the actions row stops wrapping onto a second line on all 56 screens, and PipeFlow's arrows so they point right instead of down.
- 2026-08-27: found and documented a fourth Quasar/Tailwind cascade collision — Quasar's unlayered `.flex` sets `flex-wrap: wrap`, so `flex-nowrap` never wins and `justify-between` rows silently stack once the copy gets long.
- 2026-08-25: rebased the data-source work onto the real account-scoped sources/destinations/pipelines API, dropped the local Scalar mock-server mode, and made "real" the default data source.
- 2026-08-25: cut the dashboard over to talking only to the backend — deleted the browser events SDK, its consent banner, the /events-demo page and the /japi dev proxy, and rewired Live Events and the connector catalog onto new drafted /v1/events, /v1/event-streams and /v1/connectors endpoints.
- 2026-08-25: moved every toast notification from a bottom banner to a stacking top-right panel, restyled to the Sfere ink/shadow/motion tokens.
