# Done / features / tasks log

Reverse-chronological, one line per entry: `- YYYY-MM-DD: <concise description>`. Append
whenever a feature or task is added, finished, or stopped — see CLAUDE.md's "Done-features log"
section for the full convention. Cleared weekly after being shared to Slack.

- 2026-08-31: Fixed the four general QA notes — the nav toggle under 1024px was white-on-white and is now a visible SfereIconButton, paragraph rows no longer sit 8px above the control beside them, icon-button tooltips stop being clipped at the right edge, and em dashes are gone from UI copy in favour of sentences plus a four-word empty-value vocabulary.
- 2026-08-31: Worked the Sources QA notes — install tabs now narrow to the methods that fit the source, snippets point at console.sfere.io, the off toggle no longer reads as disabled, Delete says what it does instead of promising a trash, and every Settings action with no endpoint behind it is disabled rather than toasting.
- 2026-08-29: Row-level Pause/Enable on the twelve list screens that have one now opens the same confirmation dialog Delete already did, each with copy naming what stops and where, so no state change on a table row fires straight off the click any more.
- 2026-08-29: Fixed the destination detail page throwing at render on a stat card that counted a `warehouses` list the managed-warehouse rework had deleted, and rebuilt that row the way the source page's was — Status, Inbound pipes and Created, dropping three fixture-only cards that reported a measured 0 and a literal `vundefined` on every real destination; the inbound-pipes table now reads the account's real pipelines instead of an unwired composable that always answered empty.
- 2026-08-29: Every detail, create and trash screen now carries a back link to the list it came from, rendered by PageHeader off a new `parent` field in the screen manifest — replacing the twenty-odd hand-rolled "All sources" / "Cancel" header buttons that pointed at the same list under four different names, and that only some screens had at all; top-level screens deliberately get none, since the sidebar is their nav.
- 2026-08-29: Source, Destination and Pipe detail headers now use icon-only Pause/Enable and Delete buttons, and both ask for confirmation in a dialog that spells out what the action does.
- 2026-08-29: Creating a web or Zid source now plays a short full-screen overlay animating the source → pipe → destination chain the backend provisioned, with the inline panel kept as the durable record.
- 2026-08-29: Removed the Template tab from the source detail page, along with the "Upgrade available" badge, the "Upgrade template" button and the two template rows in the Overview facts. The backend takes a `template_id` when a source is created and never returns one on the read, so all of it was permanently empty on a real source and only ever rendered off the wider mock fixture.
- 2026-08-29: Fixed the source detail page reporting "CONFIG VERSION vundefined" and three dashes — all four stat cards read fixture-only fields the backend's `Source` never returns, so the row is now Status, Pipes (the same joined list the "Destinations & pipes" tab counts, dashed while that read is in flight or failed) and Created; the Overview facts stopped labelling every real source "Hand-configured" and the Template tab stopped claiming it "was configured by hand" when the backend simply records no template id.
- 2026-08-27: Creating a source now shows what the backend already built for you — step 3 of /sources/new reveals the auto-provisioned ClickHouse destination and the pipe joining it in an animated source → pipe → destination diagram, and the primary action stops saying "Add a destination" for a source that already has one. Measured the behaviour against staging first: only `web` and `zid` sources get a destination and pipe; `event_stream` (iOS/Android/HTTP API) and `cloud_app` (Shopify/Stripe) get a write key and nothing else, so the panel is driven by what it finds rather than by the source type.
- 2026-08-27: Fixed the destination detail page telling every ClickHouse destination it "delivers over the network rather than into a warehouse" — it read `warehouseConnections`, a mock-fixture field with no counterpart in the real schema, so it now reads the real `clickhouse_database` and `config.hosts`/`protocol` and distinguishes provisioned from still-provisioning. `/dwh-connections` now reports "No API yet" instead of claiming there are no connections; the account-level ClickHouse has no API representation at all yet.
- 2026-08-27: Trash and New are now icon buttons in every list screen's toolbar, drawn by two new kit components — `SfereIconButton` (label required, feeding both the aria-label and the tooltip) and `SfereIcon` over a shared glyph registry.
- 2026-08-27: Removed the unlabelled template-version date from every card in the destination catalog — it is only meaningful next to an existing destination's version, which is where the upgrade badge already shows it.
- 2026-08-27: Dropped ClickHouse from the destination create catalog — we provision it with every plan, so it is now a highlighted notice above the grid instead of a card offering to create one.
- 2026-08-27: Fixed the write key never appearing after creating a source — `useSourcesAPI().create()` returned the backend's raw snake_case payload, so the install guide read `writeKey` off a record that only had `write_key` and showed "provisioning…" plus a `your-write-key` placeholder for a key that had been issued.
- 2026-08-27: Fixed the red CI on the ui-overhaul PR — a Markdown hard wrap had landed inside an inline code span in `docs/ui-conventions.md`, which ends the list item and makes oxfmt non-idempotent, so `oxfmt --check` failed even straight after running the formatter.
- 2026-08-27: Fixed ~1850px of dead space under step 1 of /sources/new — a `@container` inside a Quasar wrapping column flex was measured at min-content height.
- 2026-08-27: Submit rows across the create forms are now sticky to the bottom of the viewport via a new `StickyActionBar` primitive.
- 2026-08-27: replaced the header account chip's bundled photo — which showed the same stranger's face to every signed-in user — with a stroke-drawn person mark on the brand tint.
- 2026-08-27: fixed the spacing, alignment and icons on the source intent picker — redrew the six marks as real multi-element SVGs (the server rack read as a hamburger menu, the plug as a light bulb), and found two more Quasar/Tailwind cascade collisions doing it: `mt-*` on a `<p>` computes to zero so the cards' whole spacing rhythm had never rendered, and an `auto-fit` grid inflated 219px cards to 637px.
- 2026-08-27: made the post-auth "setting up your account" transition a fixed 2.5s instead of a random 1.1–2s, so it is long enough to read rather than flashing past.
- 2026-08-27: fixed the Pipes screens rendering fixture-only fields against the real pipeline API — the backend's Pipeline record has no `version`, no end names and no delivery counter, so `usePipes` now joins each pipe's source and destination in from the live Sources/Destinations collections, the "vundefined" Version card is gone, and the fabricated "0 deliveries" and "Pass-through" now read as unknown, and the Sources list's events/hour column no longer reports a measured 0 for a counter the backend never sends.
- 2026-08-27: built the screens from the onboarding proposal — Team & roles (with the domain-match approval queue) and Billing as a new ACCOUNT sidebar section, a guided three-step source flow (intent picker → details → multi-platform install & confirm), a derived setup tracker on the Dashboard with one-line reminders on Sources/Destinations/Pipes, source-detail Settings and Destinations & pipes tabs, a 29-template destinations catalog grouped by category with Included/Add-on marks, a redesigned login and an animated post-auth "setting up your account" transition.
- 2026-08-27: made the connector catalog connectable — picking a card now opens a real credentials form (named fields for Firebase, MongoDB, Shopify, Stripe and GA4, generic JSON otherwise) with a sync schedule and working validation, instead of a "coming soon" toast.
- 2026-08-27: fixed PageHeader and ToolbarSearch so the actions row stops wrapping onto a second line on all 56 screens, and PipeFlow's arrows so they point right instead of down.
- 2026-08-27: found and documented a fourth Quasar/Tailwind cascade collision — Quasar's unlayered `.flex` sets `flex-wrap: wrap`, so `flex-nowrap` never wins and `justify-between` rows silently stack once the copy gets long.
- 2026-08-25: rebased the data-source work onto the real account-scoped sources/destinations/pipelines API, dropped the local Scalar mock-server mode, and made "real" the default data source.
- 2026-08-25: cut the dashboard over to talking only to the backend — deleted the browser events SDK, its consent banner, the /events-demo page and the /japi dev proxy, and rewired Live Events and the connector catalog onto new drafted /v1/events, /v1/event-streams and /v1/connectors endpoints.
- 2026-08-25: moved every toast notification from a bottom banner to a stacking top-right panel, restyled to the Sfere ink/shadow/motion tokens.
